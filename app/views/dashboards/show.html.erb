<div class="page-header">
  <h1><%= @user.email %>'s Dashboard</h1>
</div>

<section>
  <h2>My Galleries</h2>

  <p><%= link_to "New Gallery", new_gallery_path %><p>

  <%= render_list(@user.galleries) || "You have no galleries." %>
</section>

<section>
  <h2>My Groups</h2>

  <p><%= link_to "New Group", new_group_path %></p>

  <%= render_list(@user.groups) || "You have no groups." %>
</section>
