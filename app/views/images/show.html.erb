<h1><%= @image.name %></h1>
<p><%= @image.description %></p>
<div>
  <%= link_to "Edit Image", edit_gallery_image_path(@gallery, @image) %>
  <%= link_to "Delete Image", gallery_image_path(@gallery, @image), method: "delete", data: { confirm: "Are you sure?" } %>
</div>
<%= image_tag @image.url %>
<div>
  <%= link_to @gallery.name, gallery_path(@gallery.id) %>
</div>
