<h1><%= @gallery.name %></h1>

<p> <%= @gallery.description %></p>

<%= link_to "Delete this Gallery", gallery_path(@gallery), method: :delete, data: { confirm: "Are you sure?" } %>

<%= link_to "Edit this Gallery", edit_gallery_path(@gallery) %>
